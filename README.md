Adds timestamps to movies in vlc media player. Create a dictionary of timestamps(sample of timestamps for Hamilton movie can be found in reddit and is also used as example in timestamp.py file). Then press spacebar to copy timestamps to clipboard, press it again to copy the chapter's name (the popup window box must be selected).

install python and import pygame and pyperclip
